# LiftMix

Powder-release bottle cap, 3D-printed prototype. Start with `NEXT_REVISION.md` for the current task, then `README.md` for the v3 design and `source/` for the authoritative geometry. The v3 baseline is tagged `v3`.
